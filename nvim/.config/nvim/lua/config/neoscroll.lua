-- Taken from the karb94/neoscroll repo
-- configuration for smooth scrolling

require('neoscroll').setup({
    easing_function = "sine", -- Default easing function
    -- Set any other options as needed
    hide_cursor = true
})
